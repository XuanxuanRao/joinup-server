joinup:
  db:
    host: ${DB_HOST}
    port: 3306
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    database: file-service-dev
  redis:
    host: ${REDIS_HOST}
    port: ${REDIS_PORT}
    password: ${REDIS_PASSWORD}
  nacos:
    addr: ${NACOS_ADDR}
    namespace: ""
  aliyun:
    oss:
      endpoint: oss-cn-beijing.aliyuncs.com
      accessKeyId: ${OSS_ACCESS_KEY_ID}
      accessKeySecret: ${OSS_ACCESS_KEY_SECRET}
      bucketName: joinup