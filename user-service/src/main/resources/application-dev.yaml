joinup:
  db:
    host: ${DB_HOST}
    port: 3306
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    database: joinup-user-service-dev
  redis:
    host: ${REDIS_HOST}
    port: 6379
    password: ${REDIS_PASSWORD}
    database: 0
  jwt:
    location: classpath:joinup.jks
    alias: joinup
    password: ${JWT_PASSWORD}
    tokenTTL: 180m
  aes:
    key: ${AES_KEY}
    iv: ${AES_IV}
  wx:
    miniapp:
      app-id: ${APP_ID}
      app-secret: ${APP_SECRET}